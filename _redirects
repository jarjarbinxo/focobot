# Focobot — Netlify / Cloudflare Pages redirect rules

# API functions passthrough (Cloudflare Pages Functions)
/api/*    /api/:splat    200

# SPA fallback — serve index for all unmatched routes
/*    /index.html    200
